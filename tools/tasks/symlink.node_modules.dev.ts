import {APP_DEST, NODE_MODULES_DIR} from '../config';

export = function symlinkNodeModulesDev(gulp, plugins) {
  let symlink = require('gulp-sym');
  return function () {
    return gulp.src(NODE_MODULES_DIR)
      .pipe(symlink(APP_DEST + '/' + NODE_MODULES_DIR));
  };
}
